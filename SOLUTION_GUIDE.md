# 微信一键登录“云函数未部署”问题排查与解决指南

## 🌟 问题核心诊断

根据报错信息“云函数未部署”以及您提供的后台截图（截图显示 `auth` 等四个云函数**实际上已经部署成功**），问题的根本原因在于：**小程序前端代码（`weapp/app.js`）在初始化云环境时，没有填入正确的、与您后台对应的【云开发环境 ID】。**

目前代码中的状态是：
```javascript
// weapp/app.js 的第 17 行附近
wx.cloud.init({
  env: 'YOUR_CLOUD_ENV_ID', // 🔴 这里是一个占位符，不是真实的 ID
  traceUser: true
});
```
由于使用的是占位符，小程序去请求云函数时，找不到对应的服务器环境，因此误报“云函数未部署”。

---

## 🛠️ 第一步：获取您的真实云开发环境 ID

你需要登录到您的腾讯云或微信小程序云开发控制台，找到对应的环境 ID（Environment ID）。

**获取方式：**
1. 打开**微信开发者工具**。
2. 点击顶部工具栏的 **【云开发】** 按钮，打开云开发控制台（也就是您第二张截图的那个界面）。
3. 在云开发控制台的顶部导航栏，点击 **【设置】**（右上角齿轮图标）。
4. 在“环境设置” -> “环境名称”的下方，您会看到一个 **【环境ID】**。
   * 环境 ID 的格式通常类似于：`aiae-env-1234abcd` 或者包含随机字母数字的字符串。
5. **复制** 这个环境 ID。

---

## 💻 第二步：替换代码中的环境 ID

1. 在您的代码编辑器中，打开 `/weapp/app.js` 文件。
2. 找到 `onLaunch` 函数中的 `wx.cloud.init` 方法（大约在第 17 行）。
3. 将 `'YOUR_CLOUD_ENV_ID'` 替换为您刚才复制的真实环境 ID。

**修改后的代码示例：**
```javascript
    wx.cloud.init({
      env: 'aiae-env-1234abcd', // ✅ 替换成了您真实的环境ID
      traceUser: true
    });
```
4. 保存 `app.js` 文件。

---

## 🔄 第三步：同步本地 project.config.json （可选但推荐）

在 `/weapp/project.config.json` 文件中，也可以配置默认的云函数环境。不过微信开发者工具最新版主要依赖 `app.js` 里的初始化。但如果您的开发者工具报错，您可以确认一下 `project.config.json` 里是否有 `cloudfunctionTemplateRoot` 或直接在开发者工具里右键 `cloudfunctions` 文件夹，选择“当前环境”为你对应的环境。

---

## 🧪 第四步：清理缓存并重新编译测试

代码修改完成后，在**微信开发者工具**中：
1. 点击顶部工具栏的 **【清缓存】** -> 选择 **【清除全部缓存】**。
2. 点击 **【编译】** 按钮重新加载小程序。
3. 再次点击“微信一键登录”，如果此时没有报错并且能顺利登录或跳转完善资料页面，说明问题已解决！

---

## 🔍 其他可能导致该问题的排查项（备用方案）

如果您修改了环境 ID 后**仍然**报相同的错误，请按照以下步骤排查：

### 1. 云函数未安装 npm 依赖（node_modules）
虽然截图显示“已部署”，但如果您是在本地右键“上传并部署（不包含 node_modules）”，而云端又没有开启自动安装依赖，云函数在执行时会报错，小程序端也会抛出类似未找到的错误。
**解决办法：**
在微信开发者工具中，右键点击 `cloudfunctions/auth` 文件夹，选择 **“上传并部署：云端安装依赖（不上传 node_modules）”**。

### 2. 基础库版本过低
`app.js` 中提示了需要基础库版本 2.2.3 或以上。
**解决办法：**
在微信开发者工具的右上角“详情” -> “本地设置”中，检查并调整“调试基础库”至最新版本。

### 3. 多环境冲突
如果您在云开发后台创建了**多个环境**（比如一个测试环境、一个生产环境），请确保您部署云函数所在的那个环境，和您在 `app.js` 填入的环境 ID 是**完全一致**的。

祝您明天的实验测试一切顺利！
